const Button = ({ styles }) => (
  <button
    type="button"
    className={`py-4 px-6 font-poppins font-medium  text-[18px] bg-green-gradient border-2 border-gn rounded-[10px] ${styles}`}
  >
    Contactanos
  </button>
);

export default Button;
