const ButtonAccess = ({ styles }) => (
  <button
    type="button"
    className={`py-2 px-4 font-poppins font-medium text-[12px] text-primary bg-blue-gradient rounded-[10px] outline-none ${styles}`}
  >
    Acceso
  </button>
);

export default ButtonAccess;
